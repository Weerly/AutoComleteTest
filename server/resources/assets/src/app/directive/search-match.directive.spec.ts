import { SearchMatchDirective } from './search-match.directive';

describe('SearchMatchDirective', () => {
  it('should create an instance', () => {
    const directive = new SearchMatchDirective();
    expect(directive).toBeTruthy();
  });
});
